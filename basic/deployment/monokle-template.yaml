id: io.kubeshop.templates.basic.deployment
version: 0.0.1
name: Deployment Template
description: Creates a simple Deployment and Service for the specified image
target:
  - project
  - resource
type: plain
formSchema: form-schema.json
formUiSchema: form-ui-schema.json
fileNameMappings:
  deployment.yaml: ${formData.name}-deployment.yaml
